# SakuraAdmin 领域上下文

SakuraAdmin 是一个管理后台模板：Spring Boot 3 后端（`springboot3_init`，MySQL + PostgreSQL 双库）+ Vue3/naive-ui 前端（`naive-vue-app`）。本上下文覆盖管理后台的访问控制领域 —— 谁登录、能看什么、能做什么。

## Language

**RBAC（基于角色的访问控制）**：
本项目的权限模型。用户通过「角色」获得一组「权限点」；权限校验发生在管理后台的接口与前端导航上。
_Avoid_: 权限系统、权限框架、acl

**权限点 permission**：
授权的最小单位。单表存储，通过 `type` 区分三类：`menu` / `button` / `api`。每个权限点携带权限码。
_Avoid_: 菜单项、按钮项、接口权限（单指某一类时）

**菜单权限（type=menu）**：
管理后台侧边栏导航与路由的来源。含 `parent_id`（父子层级）、`path`、`component`、`icon`、`sort`。由后端下发，前端据此动态生成路由与菜单 —— 前端不再硬编码导航。
_Avoid_: 路由表、导航项

**隐藏菜单（visible=0）**：
`visible` 字段为 0 的菜单：仍在菜单树中（路由可注册、权限码有效、可分配给角色），仅前端侧边栏渲染时跳过。与「禁用」（status=0，侧边栏/路由/权限一并下线）是两个独立维度 —— 隐藏≠禁用。隐藏一个目录菜单时，其整棵子树一并隐藏。`visible` 仅对 `type=menu` 有意义。
_Avoid_: 下架菜单、软隐藏菜单

**菜单父级规则**：
菜单的父级只能是「另一个菜单」或顶层（根）；按钮（type=button）的父级只能是其绑定的菜单，编辑时可换绑；接口（type=api）固定为顶层（parent_id=0）。父级变更必须通过存在性校验与防环校验（不能挂到自身或其子孙下）。
_Avoid_: 任意父子、接口挂菜单下

**接口权限（type=api）**：
后端接口的访问许可，以权限码标注在接口上（`@RequirePermission(code)`）。无权限码则拒绝（超管除外）。
_Avoid_: url 权限、端点权限

**按钮权限（type=button）**：
前端操作按钮（新增/删除/导出等）的显示许可，通过 `v-permission` 指令控制。语义上与对应接口权限共用一套权限码风格。
_Avoid_: 操作权限、功能权限

**权限码**：
权限点的唯一标识，冒号分层，如 `system:user:list`、`system:user:delete`。格式 `<模块>:<实体>:<动作>`。

**角色 role**：
权限点的集合。用户与角色多对多（`user_role` 关联表），角色与权限点多对多（`role_permission` 关联表）。角色分派给用户后，用户获得其并集权限。
_Avoid_: 用户类型、权限组、岗位

**超管 superadmin**：
预置角色（`admin`），默认拥有全部权限点，权限校验直接放行。超管角色不参与权限点分配逻辑。
_Avoid_: 管理员、超级管理员（管理后台所有登录者都可称"管理员"，易混淆）

**普通用户角色**：
预置角色（`user`），管理后台登录用户若未分配其他角色，至少属于该角色。仅表示"能登录后台"，不代表任何业务权限。

**数据权限**：
**本项目不做行级数据权限**。所有管理接口要么有权限码可访问、要么拒绝；不提供"仅本人/仅本部门"的行过滤。`role` / `permission` 表不预留 `data_scope` 字段。
_Avoid_: 数据范围、行级权限

**封禁 ban**：
是用户的**状态**（`User.status`），不是角色。已从角色体系移除；封禁用户不能登录后台。
_Avoid_: 封禁角色、ban 角色
