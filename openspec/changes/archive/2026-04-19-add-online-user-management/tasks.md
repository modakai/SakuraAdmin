## 1. 后端在线会话基础能力

- [x] 1.1 设计在线会话 DTO/VO/查询请求对象，覆盖 sessionId、用户 ID、账号、昵称、角色、登录 IP、客户端信息、登录时间、最近访问时间和过期时间
- [x] 1.2 扩展 token 配置或常量，新增在线会话详情 key、登录用户快照 key、最近访问时间刷新间隔等 Redis 命名
- [x] 1.3 实现在线会话存储服务，支持登录写入、最近访问时间刷新、按 sessionId 查询、分页筛选和删除会话，并复用现有 token 登录态枚举在线用户
- [x] 1.4 扩展 `TokenManager` 或新增协作服务，支持根据 sessionId 对应的 userId 删除 token、用户映射和在线会话元数据
- [x] 1.5 在登录成功流程写入在线会话元数据，并记录登录 IP、客户端信息和登录时间
- [x] 1.6 在用户注销流程同步删除当前 token 对应的在线会话记录
- [x] 1.7 在登录拦截器中按刷新间隔更新在线会话最近访问时间，避免每次请求都写 Redis

## 2. 后台接口与权限审计

- [x] 2.1 新增后台在线用户控制器，提供在线用户分页查询接口
- [x] 2.2 新增强制下线请求对象和接口，按 sessionId 执行强制下线
- [x] 2.3 为在线用户查询和强制下线接口接入后台权限校验
- [x] 2.4 在强制下线服务层阻止管理员下线当前请求对应的自身会话
- [x] 2.5 为强制下线接口补充审计日志注解或审计调用，记录操作人、目标用户、目标 IP、sessionId 和执行结果
- [x] 2.6 统一强制下线失败提示，覆盖会话不存在、会话已失效、权限不足和当前会话不允许下线

## 3. 前端接口与页面

- [x] 3.1 新增 `online-user.type.ts`，定义在线用户查询参数、列表项、分页结果和强制下线请求类型
- [x] 3.2 新增 `online-user.api.ts`，封装在线用户分页查询和强制下线接口
- [x] 3.3 新增在线用户管理页面，提供筛选表单、分页表格、刷新按钮和在线状态空状态
- [x] 3.4 在列表操作列加入强制下线按钮，并通过确认弹窗避免误操作
- [x] 3.5 强制下线成功后显示成功提示并刷新列表，失败时展示后端业务提示
- [x] 3.6 将在线用户管理页面加入路由/菜单配置，并按现有权限或角色规则控制可见性

## 4. 测试与验证

- [x] 4.1 为在线会话存储服务补充后端测试，覆盖写入、查询、筛选、刷新、删除、复用登录用户快照和避免冗余 Redis 索引
- [x] 4.2 为强制下线服务补充后端测试，覆盖成功下线、会话不存在、禁止下线当前会话和 token 失效
- [x] 4.3 为后台在线用户接口补充权限相关测试，覆盖无查看权限和无强制下线权限
- [x] 4.4 为前端在线用户 API 和页面交互补充测试，覆盖查询、空状态、确认下线和成功刷新
- [x] 4.5 执行后端测试，确认登录、注销、当前用户获取和在线用户管理流程没有回归
- [x] 4.6 执行前端类型检查和测试，确认新增页面、接口类型和菜单配置可用
