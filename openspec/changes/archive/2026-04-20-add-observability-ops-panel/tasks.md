## 1. 后端模块与依赖

- [x] 1.1 在 `pom.xml` 中新增 Actuator、Micrometer Prometheus Registry 和 OSHI 依赖，并保留中文依赖注释。
- [x] 1.2 新建 `com.sakura.boot_init.observability` 模块目录和 `package-info.java`，声明 Spring Modulith 模块名与允许依赖。
- [x] 1.3 新增 `ObservabilityProperties` 配置类，覆盖慢接口阈值、登录失败窗口、告警冷却时间、事件保留时间和资源告警阈值，并为配置项添加中文注释。
- [x] 1.4 配置 Actuator 暴露范围，确保前端只使用管理员聚合接口，不直接依赖 `/actuator/**`。

## 2. 系统状态聚合

- [x] 2.1 实现 JVM、线程、GC、堆内存和非堆内存采集服务，输出统一 VO。
- [x] 2.2 使用 OSHI 实现系统 CPU、进程 CPU、系统内存和磁盘信息采集。
- [x] 2.3 实现数据库健康检查和 Hikari 连接池状态聚合。
- [x] 2.4 实现 Redis 连通性、延迟和基础状态检查。
- [x] 2.5 新增管理员系统状态接口，返回状态等级、采样时间和依赖可用性。

## 3. 接口观测与事件存储

- [x] 3.1 新增运维事件实体、枚举、Mapper 和必要索引，覆盖慢接口、错误事件、安全告警和强制下线事件。
- [x] 3.2 实现请求观测过滤器或拦截器，采集路径、方法、状态码、耗时、用户、IP 和异常摘要。
- [x] 3.3 实现敏感字段脱敏和摘要长度限制，禁止保存 token、密码、验证码、密钥和完整堆栈。
- [x] 3.4 实现慢接口判定和异步写入，记录超过阈值的接口事件。
- [x] 3.5 实现错误事件写入和按时间桶聚合的错误趋势查询。
- [x] 3.6 新增接口监控管理员接口，提供耗时摘要、错误趋势和慢接口分页查询。

## 4. 安全事件与告警联动

- [x] 4.1 确认登录失败事实来源，并通过审计事件、审计 API 扩展或认证公开事件接入观测模块。
- [x] 4.2 使用 Redis 实现 IP、账号和 IP 多账号维度的登录失败滑动窗口统计。
- [x] 4.3 实现异常 IP 识别、登录失败安全事件落库和管理员查询接口。
- [x] 4.4 接入强制下线记录来源，确保安全事件页面可以查看强制下线记录。
- [x] 4.5 实现高频登录失败告警规则、冷却去重和审计事实提交。
- [x] 4.6 发布 `ObservabilityAlertRaisedEvent` 之类的告警事件，并在通知模块中监听生成系统通知。

## 5. 前端运维页面

- [x] 5.1 新增 `observability.type.ts`，定义系统状态、接口监控、安全事件和告警相关类型。
- [x] 5.2 新增 `observability.api.ts`，使用 TanStack Vue Query 封装系统状态、慢接口、错误趋势和安全事件查询。
- [x] 5.3 新增系统状态页面，展示 JVM、CPU、内存、磁盘、数据库和 Redis 状态。
- [x] 5.4 新增接口监控页面，展示耗时摘要、错误趋势图和慢接口列表。
- [x] 5.5 新增安全事件页面，展示登录失败统计、异常 IP、强制下线记录和关联审计/通知入口。
- [x] 5.6 接入菜单、路由和权限标识，页面文案使用后台管理员可理解的中文表达。

## 6. 验证与收尾

- [x] 6.1 增加后端模块边界或应用上下文测试，验证 `observability` Modulith 依赖不越界。
- [x] 6.2 增加系统状态聚合、慢接口判定、脱敏规则和登录失败阈值的重点单元测试。
- [x] 6.3 按前端测试规则，仅为复杂数据转换或查询归一化补充正式测试，临时验证测试用完删除。
- [x] 6.4 运行后端测试或至少编译校验，运行前端 `pnpm build` 校验页面类型和构建。
- [x] 6.5 检查所有新增 Java 代码是否包含必要中文注释，避免无注释公共类、配置项和关键规则。
