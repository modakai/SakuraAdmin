## 1. 数据模型与迁移

- [x] 1.1 设计通知内容表字段，覆盖类型、标题、摘要、内容、级别、状态、接收端范围、目标范围、生效时间、失效时间、置顶、弹窗和审计字段
- [x] 1.2 设计通知目标表或等价结构，支持系统后台用户、用户端用户、全部用户三类接收端范围，以及全部用户、指定角色和指定用户三类目标范围
- [x] 1.3 设计通知阅读状态表，记录通知 ID、用户端类型、用户 ID、已读时间和关闭时间
- [x] 1.4 设计消息通知模板表，记录模板编码、事件类型、标题模板、内容模板、变量定义、启用状态和审计字段
- [x] 1.5 在后端 SQL 初始化或迁移脚本中新增通知公告、通知目标、阅读状态和消息模板相关表及查询索引

## 2. 后端通知领域实现

- [x] 2.1 新增 `notification` 领域包结构，包含 controller、service、repository、entity、dto、vo 和枚举定义
- [x] 2.2 实现管理端通知公告创建、编辑、分页查询、详情查询、发布、撤回和归档接口
- [x] 2.3 实现通知生命周期状态校验，阻止已撤回或已归档通知被非法发布
- [x] 2.4 实现接收端范围、目标范围保存和当前访问端用户可见性匹配逻辑
- [x] 2.5 实现消息通知模板创建、编辑、分页查询、详情查询、启用和停用接口
- [x] 2.6 实现模板变量解析、变量定义校验和模板内容渲染逻辑
- [x] 2.7 实现禁用用户等系统事件调用模板自动生成通知消息的服务入口
- [x] 2.8 实现后台端和用户端通知消息列表、详情、未读计数、单条已读和全部已读接口
- [x] 2.9 实现后台端和用户端公告查询、公告关闭或已读接口，并按接收端范围、生效时间、失效时间、置顶和弹窗规则过滤
- [x] 2.10 为管理端接口接入管理员权限校验，为后台端和用户端接口接入登录用户身份和可见范围校验

## 3. 前端管理端实现

- [x] 3.1 新增通知公告和消息模板 TypeScript 类型与 API 客户端，覆盖管理端、后台端和用户端接口
- [x] 3.2 新增管理端通知公告列表页，支持类型、状态、接收端范围、标题和发布时间筛选
- [x] 3.3 新增通知公告表单页或弹窗，支持草稿创建、编辑接收端范围、目标范围和展示配置
- [x] 3.4 在管理端列表中接入发布、撤回、归档和详情查看操作，并处理成功后的查询刷新
- [x] 3.5 新增消息通知模板管理页面，支持模板编码、事件类型、标题模板、内容模板、变量定义、启用和停用
- [x] 3.6 将通知公告管理和消息模板管理入口接入现有管理端菜单和路由

## 4. 前端用户端展示

- [x] 4.1 新增后台端通知入口组件，展示后台用户未读计数并支持打开通知列表
- [x] 4.2 新增用户端通知入口组件，展示用户端用户未读计数并支持打开通知列表
- [x] 4.3 新增通知消息列表和详情展示，支持单条已读和全部已读操作
- [x] 4.4 新增公告展示组件，支持按接收端范围展示置顶公告、弹窗公告和关闭后不重复弹出
- [x] 4.5 将后台通知入口接入管理端全局布局，将用户端通知入口和公告展示接入合适的用户端页面
- [x] 4.6 处理撤回、过期、无权限、模板未启用和空列表等前端状态

## 5. 测试与验证

- [x] 5.1 为后端通知生命周期、接收端范围匹配、目标范围匹配、模板变量渲染、未读计数和公告有效期过滤补充测试
- [x] 5.2 为禁用用户自动通知场景补充测试，验证 `{reason}` 等变量被正确替换且缺失变量时不发送
- [x] 5.3 为前端通知 API 客户端、模板 API 客户端、管理端表单校验和用户端已读交互补充测试
- [x] 5.4 执行后端测试，确认通知公告接口、模板接口和既有模块没有回归
- [x] 5.5 执行前端测试和构建，确认路由、组件和类型检查通过
- [x] 5.6 使用 OpenSpec 校验变更状态，确认 proposal、design、specs 和 tasks 均可用于实施
