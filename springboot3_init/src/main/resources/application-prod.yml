# 线上配置文件
# @author sakura
# @from sakura
server:
  port: 8101
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    todo 注意修改成docker的服务名
    url: jdbc:mysql://mysql:3306/my_db
    username: root
    password: 123abc
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: redis
    port: 6379
    timeout: 5000
    password:
mybatis-flex:
  configuration:
    # 生产环境关闭日志
    log-impl: ''
# 接口文档配置
knife4j:
  basic:
    enable: true
    username: root
    password: 123456
